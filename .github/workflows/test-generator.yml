name: Test Generator Script

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  JAVA_VERSION: '21'

jobs:
  test:
    name: Generate & Build Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          check-latest: true

      - name: Configure git identity
        run: |
          git config --global user.email "test@example.com"
          git config --global user.name "Test Runner"

      - name: Run generator script with automated inputs
        run: |
          printf 'TestApp\ncom.example.testapp\n./test-app\n24\n\n\n#FFFFFF\nY\n' | bash create-android-app.sh

      - name: Detect compileSdk from generated project
        id: detect-sdk
        run: |
          COMPILE_SDK=$(grep -oP 'compileSdk\s*=\s*\K\d+' test-app/app/build.gradle.kts)
          echo "compileSdk=${COMPILE_SDK}" >> "$GITHUB_OUTPUT"

      - name: Install Android SDK platform
        env:
          SDK: ${{ steps.detect-sdk.outputs.compileSdk }}
        run: |
          SDKMAN="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          if yes | "$SDKMAN" "platforms;android-${SDK}" 2>&1; then
            echo "SDK_INSTALLED=$SDK" >> "$GITHUB_ENV"
          else
            echo "android-${SDK} not available, finding latest..."
            AVAILABLE=$("$SDKMAN" --list 2>/dev/null | grep -oP 'platforms;android-(\d+)' | grep -oP '\d+' | sort -rn | head -1)
            echo "Installing android-${AVAILABLE} instead..."
            yes | "$SDKMAN" "platforms;android-${AVAILABLE}" 2>&1
            echo "SDK_INSTALLED=$AVAILABLE" >> "$GITHUB_ENV"
            sed -i -E "s/compileSdk\s*=\s*${SDK}/compileSdk = ${AVAILABLE}/" test-app/app/build.gradle.kts
            sed -i -E "s/targetSdk\s*=\s*${SDK}/targetSdk = ${AVAILABLE}/" test-app/app/build.gradle.kts
          fi

      - name: Build generated project (assembleDebug)
        working-directory: ./test-app
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload build reports (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-reports
          path: test-app/app/build/reports/
          retention-days: 3
